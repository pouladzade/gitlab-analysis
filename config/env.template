# GitLab Analysis Tool Configuration Template
# Copy this file to .env in the project root and fill in your actual values

# GitLab Instance Configuration
# Your GitLab instance URL (without trailing slash)
GITLAB_URL=https://gitlab.ptc-telematik.de

# GitLab Personal Access Token
# Generate at: GitLab → Profile → Access Tokens → Personal Access Tokens
# Required scopes: api, read_repository
GITLAB_TOKEN=your_personal_access_token_here

# Optional: Default analysis period in days
DEFAULT_ANALYSIS_DAYS=60

# Optional: Default author filter (comma-separated emails or names)
# DEFAULT_AUTHORS=john.doe@company.com,jane.smith@company.com

# Optional: Repository exclusions (comma-separated repository names)
# EXCLUDE_REPOSITORIES=archived-repo,test-repo

# Optional: File type filters for code analysis
# CODE_FILE_EXTENSIONS=.py,.js,.java,.cpp,.c,.h,.cs,.php,.rb,.go,.ts,.jsx,.vue,.html,.css,.scss,.sql,.yaml,.yml,.json,.xml

# Optional: Output directory customization
# REPORTS_DIRECTORY=gitlab_reports
# PROJECTS_DIRECTORY=projects

# Optional: Logging configuration
# LOG_LEVEL=INFO
# LOG_FILE=gitlab_analysis.log 